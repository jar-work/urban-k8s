# External Secrets Operator - Dev

installCRDs: true

serviceAccount:
  create: true
  name: external-secrets

webhook:
  create: true

certController:
  create: true

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi
